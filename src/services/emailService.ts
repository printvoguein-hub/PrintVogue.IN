// Empty EmailJS service file to satisfy import
// This was replaced by Supabase email system but import still exists in main.tsx

export const initEmailJS = () => {
  // No longer needed - using Supabase for email notifications
  console.log('EmailJS initialization skipped - using Supabase email system');
};